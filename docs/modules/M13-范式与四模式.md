# M13 范式与四模式（Plan-and-Solve · Reflection · 模式引擎）

| 项 | 值 |
|---|---|
| 生产进度 | Sprint 9 · 里程碑 MI-4「完整 Agent 形态」 |
| 代码落点 | `backend/agent_godot/agent/paradigms/`（4 个文件 + loop.py 小改，见 §0.5） |
| 前置模块 | M03（ReAct 循环=ask 模式底座）· M06（headless 验证=Reflection 验证器） |
| 手写比例 | 100% 手写（不用 LangGraph） |
| 教程映射 | 📘 zero2Agent 05 课 · 📝笔记 ReAct/Plan-and-Solve/Reflection · M15 前置 |

---

## 0. 本模块在项目中的位置

**大白话**：M03 造好了发动机（ReAct 循环），本模块给车装**换挡器**。同一台发动机，四种驾驶模式：**ask**（经济模式——问答巡航，M03 已有）、**craft**（施工模式——改代码必须"改完自检"）、**plan**（先测绘后施工——先出图纸人批准再动手）、**multi**（车队模式——拆任务派车队并行干）。模式不是四套代码，而是**同一循环挂不同配置**（策略对象）：允许哪些工具、温度多少、要不要前置规划、要不要验证回路——这正是 Cursor/CodeBuddy 顶部那个模式切换器的内核。

四范式在此正式化（M03 §0 全景表的下半场）：

| 范式 | 模式 | 本模块落点 |
|---|---|---|
| ReAct | ask | `AskStrategy`（M03 直改） |
| Reflection（客观验证回路） | craft | `CraftStrategy` + M06 验证器 |
| Plan-and-Solve | plan | `PlanStrategy`（DAG+审批+re-plan） |
| Multi-Agent 骨架 | multi | `MultiStrategy`（M15 完全体） |

**交付后状态**：`godot-agent plan "给游戏加存档系统"` → 出 DAG 请你审批 → 批准后逐步执行、失败节点 re-plan；craft 模式改代码自动 headless 校验不过不交卷。

---

## 0.5 ★ 施工文件清单（开工前必看的一页表）

**本模块你一共要新建 6 个文件**（+ loop.py 一处小改）：

| # | 新建文件（完整路径） | 职责一句话 | 关键类/函数 | 预估行数 | 手敲步骤(§4) | 依赖 |
|---|---|---|---|---|---|---|
| 1 | `agent/paradigms/__init__.py` | 空包+模式注册表 | `PARADIGMS` | 5 | 步骤 0 | — |
| 2 | `paradigms/base.py` | 策略基类+配置 | `ModeStrategy`、`ModeConfig` | 50 | 步骤 1 | M03 |
| 3 | `paradigms/ask.py` | ask：ReAct 直改 | `AskStrategy` | 30 | 步骤 2 | base |
| 4 | `paradigms/craft.py` | craft：验证回路 | `CraftStrategy`、`VerifyLoop` | 90 | 步骤 3 | base + M06 |
| 5 | `paradigms/plan.py` | plan：DAG+审批+re-plan | `PlanStrategy`、`PlanGraph` | 130 | 步骤 4 | base + M09 确认门 |
| 6 | `paradigms/multi.py` | multi 骨架 | `MultiStrategy`（M15 扩展点） | 50 | 步骤 5 | base |
| — | `agent/loop.py` 小改 | run() 接受 mode 参数 | `get_strategy(mode)` | +10 | 步骤 6 | base |

**完成后你拥有**：验收三连（§5）；面试可白板"四模式=一循环四策略"。

---

## 1. 知识点详解（每节五段：定义 → 大白话 → 举例 → 演进 → 易错点）

### 1.1 Plan-and-Solve（plan 模式）

**① 严格定义**：两阶段范式——先由模型产出**任务 DAG**（有向无环图：节点=子任务，边=依赖），经**人审批**（M09 确认门）后按**拓扑序**执行；节点失败触发 **re-plan**（带着失败上下文重新规划剩余部分，不推倒已完成）。提示层激发（Plan-and-Solve Prompting, Wang 2023）→ 工程层落地（本模块：结构化 DAG 而非文本计划）。

**② 大白话**：**先画施工图再盖楼**。直接砌墙（ReAct 一步一动）盖到三楼发现地基没打——返工代价惨重；plan 模式是先画依赖图（"打地基→[框架、水电]→封顶"，框架和水电可并行），**业主签字**后开工，顺序由图纸保证（拓扑排序=不拆承重先装窗户的事故免疫）。中途水电失败：不砸整栋楼重来（re-plan 只重排"未完工部分"，地基和框架保留）。

**③ 举例**：DAG 结构与执行器核心：

```python
@dataclass
class PlanNode:
    id: str; title: str; description: str
    depends: list[str]; status: Literal["pending","running","done","failed","skipped"]
    max_retries: int = 1

class PlanGraph:
    def ready_nodes(self) -> list[PlanNode]:          # 拓扑推进的核心
        """依赖全部 done 的 pending 节点（且无同层 failed 未处理）。"""
        return [n for n in self.nodes.values()
                if n.status == "pending"
                and all(self.nodes[d].status == "done" for d in n.depends)]
```

计划提示（让模型输出 JSON DAG）：要求**粒度 3~7 节点**（太细=DAG 里的 for 循环，浪费规划；太粗=失去并行机会）、每个节点写"完成判据"（给 re-plan 判断用）。

**④ 演进**：CoT 一步到位（错无回头）→ ReAct（边走边看，长任务漂移）→ Plan-and-Solve（2023：先全局规划）→ ReWOO（计划与执行解耦，计划期不消耗观察）→ LLM+PDDL（形式化规划器，选读）。工程落点：plan 审批是人类在环（HITL）范式在"任务级"的挂载。

**⑤ 易错点**：
- 模型给出的 DAG 可能**带环**（A 依赖 B 依赖 A）——解析时拓扑检测，带环直接打回重规划
- 计划幻觉（节点不可执行/依赖虚构）：每个节点审批时要人看"完成判据"是否可验证
- re-plan 上下文要带"已完成节点的产出摘要"，否则新计划会重复已完成步骤

### 1.2 Reflection 与 craft 验证回路

**① 严格定义**：Reflection（Reflexion, Shinn 2023）= 行动后**自我评估并修正**的回路。三形态按可靠性排序：**自评**（模型给自己打分——倾向高分，弱）/ **互评**（另一模型评——好些）/ **客观验证器**（外部工具可判定对错——最强，本项目采用）。craft 模式的验证器 = M06 headless 四级校验（语法/导入/测试/运行），验证不过→错误作为 Observation 回填→修正→再验，`max_fixes=3` 封顶。

**② 大白话**：**车间质检员制度**。工人（模型）加工完零件（改完代码）不能直接交货——过三坐标测量仪（headless 校验）：不合格，测量报告（错误行号）退回工人返工；三次仍不合格，上报车间主任（用户确认：继续修还是回滚）。关键认知：**质检员是仪器不是师傅**——用"客观验证器"而非"再问一遍模型"，因为模型自评像让学生给自己批卷（基本都打 90 分），仪器不说谎。

**③ 举例**：craft 执行钩子（挂在 M03 Loop 的工具后）：

```python
class VerifyLoop:
    WRITE_TOOLS = {"write_file", "godot_write_script", "godot_edit_scene"}
    def __init__(self, runner: GodotRunner, max_fixes: int = 3): ...

    async def after_write(self, tool: str, response: ToolResponse,
                          session: Session) -> str | None:
        """返回"未通过的错误摘要"或 None（通过）。Loop 把它注入下一轮。"""
        if tool not in self.WRITE_TOOLS or not response.ok:
            return None                                  # 只验成功的写
        result = await self.runner.check()               # L1 起步，配置到 L3
        if result.ok:
            return None
        self.fixes += 1
        if self.fixes > self.max_fixes:
            return ("验证连续失败超限，请向用户确认：继续修复或 /rewind 回滚。"
                    f"最后一次错误：\n{result.errors[:1500]}")
        return (f"⚠️ 写入后 headless 校验未通过（第{self.fixes}次），请修复后重写：\n"
                f"{result.errors[:1500]}")
```

**④ 演进**：无验证（改完就交）→ 自评 Reflection（弱）→ Reflexion（语言化反思+记忆，选读）→ **工具增强自修正**（2023 主流：验证器是工具）→ M18 GRPO 可验证奖励（同一思想训练化）。

**⑤ 易错点**：
- 验证成本也是钱：L1 秒级每次写都验、L3 分钟级只在关键节点验——分级配置
- `max_fixes` 触顶后**必须**升级给人（确认门），静默放弃=半成品交卷
- 验证错误回填要走 M03 的 Observation 通道（错误也是数据），别抛异常炸循环

### 1.3 四模式引擎：同一循环的策略对象

**① 严格定义**：模式=预置配置的策略对象（Strategy 模式），四维差异：**工具集**（registry.filter 视图）/ **采样参数**（temperature/top_p，M02 路由表）/ **循环钩子**（craft 挂 VerifyLoop、plan 挂 PlanGraph、multi 挂分发器）/ **系统提示模板**。`loop.py` 的 `run(mode=...)` 按注册表取策略装配——**新增模式=新策略类+注册，循环零改动**（注册表模式第三次落地）。

**② 大白话**：**换挡器不是换发动机**。运动模式（craft 低温度+验证回路）、经济模式（ask 常规）、 autopilot（plan 全局规划）、车队调度（multi）——发动机（M03 循环）从没换过，换的是油门响应（采样参数）、限速器（工具白名单）、导航仪（计划图）。这就是"模式"在 Cursor/CodeBuddy 里的真实形态：**产品概念 → 配置组合**。

**③ 举例**：四模式配置矩阵（可直抄进代码做默认值）：

```python
DEFAULT_MODES = {
 "ask":   ModeConfig(tools="readonly",  temperature=0.7, verify=None,  plan_first=False),
 "craft": ModeConfig(tools="all",       temperature=0.1, verify="L1+",  plan_first=False),
 "plan":  ModeConfig(tools="all",       temperature=0.2, verify="L1+",  plan_first=True),
 "multi": ModeConfig(tools="per_worker",temperature=0.3, verify="per_task", plan_first=True),
}
```

**④ 演进**：硬编码 if mode==（每处散落）→ 配置文件 → 策略对象+注册表（本项目）。趋势：模式进一步产品化（自定义模式=用户可写 yaml 定义自己的 ModeConfig，M19 设置页提供）。

**⑤ 易错点**：
- 模式切换在**会话内**生效但工具视图要重建（registry.filter 是视图不是拷贝）
- craft 的 temperature=0.1 与 M02 路由表是同一处配置的两个入口——单一事实源（models.yaml），别在两处写死
- plan 模式被拒绝审批后要**干净退出**（不残留半执行状态），确认门复用 M09

---

## 2. 接口设计（完整签名）

```python
# paradigms/base.py
@dataclass
class ModeConfig:
    tools: Literal["readonly", "all", "per_worker"] | list[str]
    temperature: float; top_p: float = 0.95
    verify: None | Literal["L1", "L1+", "L3", "per_task"]
    plan_first: bool = False
    system_prompt_template: str | None = None

class ModeStrategy(ABC):
    mode: str; config: ModeConfig
    def tools_view(self, registry: ToolRegistry) -> ToolRegistry: ...
    async def before_loop(self, session: Session, task: str) -> list[Message]: ...
    async def on_tool_done(self, tool: str, resp: ToolResponse,
                           session: Session) -> str | None: ...   # craft 验证注入
    async def should_continue(self, session: Session) -> bool: ...  # plan 推进控制
    def register(): PARADIGMS[cls.mode] = cls                      # 注册表

# paradigms/plan.py
class PlanGraph:
    def __init__(self, nodes: list[PlanNode]): ...
    @classmethod
    async def from_task(cls, llm: LLM, task: str) -> "PlanGraph": ...
    def validate_acyclic(self) -> None: ...            # 带环抛 PlanCycleError
    def ready_nodes(self) -> list[PlanNode]: ...
    def mark(self, node_id: str, status: str) -> None: ...
    async def replan_from(self, llm: LLM, failed: PlanNode) -> None: ...
    def render_for_approval(self) -> str: ...          # 审批视图（人看的）
```

---

## 3. 关键难点参考片段：plan 模式主循环

拓扑推进+失败处理+re-plan 的交织（挂在 M03 Loop 之外的一层外循环）：

```python
async def run_plan_mode(self, session, task: str):
    graph = await PlanGraph.from_task(self.llm, task)
    graph.validate_acyclic()
    await self.confirm.gate(                            # 人审批（M09）
        render_approval(graph, "计划已生成，批准执行？可要求修改"))
    while True:
        ready = graph.ready_nodes()
        if not ready and all(n.status in ("done","skipped") for n in graph):
            return LoopResult(final_text="全部节点完成", stop_reason="natural")
        if not ready:                                   # 有 failed 阻塞
            failed = next(n for n in graph if n.status == "failed")
            await graph.replan_from(self.llm, failed)   # 带上下文重排剩余
            continue
        for node in ready:                              # 无依赖节点并行（M15 前先串行）
            result = await self.loop.run(session, node.prompt(), mode="craft")
            graph.mark(node.id, "done" if result.ok else "failed")
```

为什么难：**推进条件**（ready 空≠结束：可能是死锁阻塞）、re-plan 的上下文构造（已完成节点的产出摘要）、审批拒绝后的退出路径——三条状态流拧在一起，测试必须逐条覆盖。

---

## 4. 手敲指引（函数级伪代码）

| 步骤 | 文件 | 函数级作用（伪代码） | 验证 |
|---|---|---|---|
| 1 | `base.py` | `ModeConfig dataclass；ModeStrategy 抽象四钩子（tools_view=registry.filter；before_loop 默认返回[]；on_tool_done 默认 None；should_continue 默认 True）+ 类注册器` | 注册表含 4 键 |
| 2 | `ask.py` | `config=readonly+0.7；全部钩子用默认实现——20 行胶水，验证"ask=M03 原样"` | 原有测试全绿（回归） |
| 3 | `craft.py` | `config=all+0.1+verify；on_tool_done=§1.2 VerifyLoop.after_write 逻辑（写类工具→runner.check→错误串/None/超限升级话术）` | 写坏代码→循环内自修→3 次后升级确认 |
| 4 | `plan.py` | `PlanGraph.from_task：任务→JSON DAG 提示→解析+校验无环+补"完成判据"；ready_nodes=依赖全 done 的 pending；replan_from：已完成摘要+失败原因→重生成剩余子图；render_for_approval：缩进树+每节点判据`；`PlanStrategy.before_loop=§3 外循环` | 样例任务 DAG 正确、审批拒绝干净退出 |
| 5 | `multi.py` | `骨架：config=per_worker+plan_first；真正的 Orchestrator 留 M15（此处仅占位+提示"子代理派发"）` | mode=multi 可跑（单代理降级） |
| 6 | `loop.py` 小改 | `run(..., mode="ask")：get_strategy=PARADIGMS[mode] → 用 strategy.tools_view/before_loop/on_tool_done 装配循环` | 四模式验收 |

---

## 5. 测试与验收

```python
async def test_craft_auto_fixes_syntax_error():
    # 假 LLM 先写错→收到验证错误 Observation→改对；断言 fixes==1 且最终 ok

async def test_plan_rejects_cyclic_dag():
    with pytest.raises(PlanCycleError):
        PlanGraph([node_a(dep=b), node_b(dep=a)]).validate_acyclic()

async def test_plan_replan_keeps_done_nodes():
    # 3 节点，第 2 失败一次 → re-plan 后节点 1 仍 done 且不被重复执行

async def test_mode_registration_and_views():
    assert set(PARADIGMS) == {"ask", "craft", "plan", "multi"}
    assert "write_file" not in PARADIGMS["ask"].tools_view(reg).names
```

**验收 Demo**：① `plan "给 sample 加存档系统"`（出 DAG→审批→执行→re-plan 演示）② `craft "写一个有语法错误的脚本再修好"`（观察验证回路三段式）③ `ask "Area2D 是什么"`（回归不碎）。

---

## 6. 踩坑记录（留白自填）

| 日期 | 坑 | 现象 | 根因 | 解法 | 关联知识点 |
|---|---|---|---|---|---|
|     |    |     |     |    |          |

---

## 7. 面试拷打（附详细参考答案）

**1. Plan-and-Solve 与 ReAct 的适用分界？**
答：任务结构决定范式选择。ReAct 强项：探索型、步骤不可预知（"看看这个项目有什么问题"——看什么取决于每步看到什么）；弱项：长任务目标漂移（20 轮后忘了初心）+ 无并行（天然串行）。Plan-and-Solve 强项：目标明确、子任务有依赖结构（"加存档系统"=可预先分解为配置/序列化/UI 三块）；弱项：探索型任务计划无意义（计划赶不上变化，re-plan 成本反噬）。工程判据：**能写出 3~7 个"完成判据明确"的子任务就用 plan，否则 ReAct**。四模式让用户/路由器（M12）按任务形态选。

**2. 为什么选"客观验证器"而不是模型自评做 Reflection？**
答：可靠性排序：客观验证器>互评>自评。自评的系统性偏差：模型给自己的输出打分倾向高分（训练目标使然——RLHF 优化的是"看起来好"），且缺乏可执行的判断标准；自评唯一的用途是低成本初筛。客观验证器（headless 校验/编译/测试）的优势：判定可复现（同输入同结论）、错误带定位（行号/断言），修复信号强——craft 模式选择它是因为**代码的"对错"恰好可机器判定**（这是领域运气：Git 代码是少数有免费验证器的领域）。反例：文案好坏无客观验证器，只能退互评/人评。

**3. DAG 为什么要人审批？审批看什么？**
答：审批是任务级的 HITL（M09 是操作级）。看三点：①**节点粒度**（3~7 个，太细说明模型在 DAG 里写 for 循环）②**完成判据**（每节点"怎么算完成"是否可验证——不可验证的判据执行时无法判定 done）③**依赖结构**（有没有"其实无依赖却被串行"的浪费并行机会/有没有幻觉依赖虚构的文件）。审批的成本收益：一次审阅 30 秒 vs 计划错误执行 10 分钟后返工——**纠错成本随执行深度指数增长**，在源头拦最便宜。

**4. re-plan 为什么不推倒重来？要带什么上下文？**
答：不推倒：已完成节点的产出（文件、配置）是真实资产，重做烧钱且可能与已落盘产物冲突（乐观锁连锁冲突）。带的上下文三样：①已完成节点的清单+产出摘要（新计划必须跳过/衔接它们）②失败节点的**完整错误上下文**（不是"失败了"三个字——错误 Observation 原文，否则新计划重蹈覆辙）③原始任务目标（防多轮 re-plan 后目标漂移）。工程细节：replan_from 只重新生成"失败节点及其后代"的子图，前驱保持。

**5. 四模式的技术本质是什么？如何新增一个模式？**
答：本质=**同一 ReAct 循环的策略对象配置**：工具集视图（registry.filter）+采样参数+循环钩子（验证回路/计划推进）+系统提示。新增模式三步：写 ModeStrategy 子类（差异化只动钩子）+注册+产品入口暴露（CLI 参数/Web 切换器）。循环代码零改动——这是策略模式+注册表（第三次复用）的组合收益。面试加分：指出"模式是产品概念不是技术分叉"——Cursor 的自定义模式本质就是让用户写自己的 ModeConfig yaml。

**6. craft 的验证为什么分级（L1 每次/L3 关键节点）？**
答：成本-收益匹配。L1 语法校验秒级、拦住最高频错误（缩进/未定义变量）——每次写后跑，性价比极高；L3 测试套件分钟级——每次写后跑会让一个 5 步任务多花 5 分钟，只在"节点完成"（plan 的节点边界）或用户显式要求时跑。分级实现：ModeConfig.verify 字段（"L1"=只语法，"L1+"=语法+导入，"L3"=加测试）。这与 CI 的分层（pre-commit 秒级/pre-push 分钟级/流水线小时级）同构——**反馈速度与检查深度的阶梯设计**。

**7. 计划幻觉（不可执行节点/虚构依赖）怎么防？**
答：四道防线：①生成时提示约束（每节点必须绑定"完成判据"+判据必须是可观察的：文件存在/校验通过/测试绿）②结构校验（无环+依赖引用的节点存在+节点数 3~7）③**人审批**（最后一道，也是最有效的——人对"这计划靠不靠谱"的直觉远超规则）④执行期容错（节点跑完判据不满足→标记 failed→re-plan，而不是硬着头皮往下走）。承认计划幻觉不可能根除（模型对项目现状认知有限），防线设计成纵深——这也是"验证文化"从代码延伸到计划的体现。

**8. multi 模式在 M13 与 M15 的分界？**
答：M13 提供骨架：模式注册（配置维度 per_worker/plan_first）+降级路径（无 Orchestrator 时退化为 plan 串行）；M15 提供血肉：Orchestrator-Worker 分发器、子代理上下文隔离、结果聚合、A2A 协议。分界的工程理由：模式引擎（配置/注册/钩子）与并行执行器（进程/任务管理）是正交关注点——分两模块各自演进，M13 完成时 multi 可用（串行版），M15 完成时变并行（接口不变）。

**9. ToT（Tree of Thoughts）与 plan 模式的关系？**
答：ToT 是 plan 的"搜索化"版本：不是生成一个计划，而是生成 k 个候选分支、每步评估打分、保留 top 分支继续扩展（beam search 式推理树）。适用：解空间大且步进可评估的任务（数学题/谜题）；不适用：工程任务（分支执行有真实副作用，回溯成本不是 token 是文件系统——M06 检查点回滚远比"换个分支"贵）。本项目的工程化近亲：plan 模式的多候选方案对比（审批视图给 2 个方案让人选）借了 ToT 的"分支生成"思想，砍掉"自动回溯"（副作用领域回溯要人批）。选读论文，思想收编，机制不用。

**10. 开放题：模式自动选择（Auto 模式）怎么设计？**
答：三段设计：①信号源——M12 意图分类已有 code_edit/knowledge 分流（Auto 的雏形），扩展信号：任务长度预估（首句+项目规模→长任务倾向 plan）、历史模式使用效果（同类任务 craft 三次失败→下次建议 plan，M08 记忆的用武之地）；②决策器——规则优先（明确 code_edit→craft）+小模型兜底（四分类，同 M12 意图分类器复用）；③透明性与否决权——Auto 的选择必须展示且一键切换（"已选 craft，切换？"）——自动化的前提是可否决，否则一次误判毁信任。演进路径：Auto 先做"建议"（弹推荐让人确认）再做"默认"（置信度>阈值才自动）——灰度思想一以贯之。

---

## 8. 教程映射与延伸

- 📘 zero2Agent 05 课（agent 范式全景）
- 必读：Plan-and-Solve Prompting（Wang 2023，短文）；ReWOO（计划/执行解耦）
- 选读：Reflexion（Shinn 2023）；ToT（Yao 2023）；LATS
